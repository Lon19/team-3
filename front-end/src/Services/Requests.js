import axios from "axios";

export const getHistory = async (user, type) => {
    //const result = await API.get(`getHistory`, { params: { user, type } });
    return [2, 2, 33, 4, 5];//result.data;
};